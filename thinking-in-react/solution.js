import React from 'react';
import ReactDOM from 'react-dom';

export const FilterableProductTable = React.createClass({
  render() {
    return (
      <div>

      </div>
    )
  }
})

export const SearchBar = React.createClass({
  render() {
    return (
        <input/> // issue here, intructions are not clear
    )
  }
})
